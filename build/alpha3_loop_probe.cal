int32 print = manual(string msg) -> {
    int32 index = 0;
    while (index < msg.length) {
        int8 ch = msg[index];
        _ = ch;
        index = index + 1;
    }
    return 0;
};

boot() -> print("hi");
