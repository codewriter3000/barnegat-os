int32 print_at = manual(string msg, int32 index) -> {
    if (index < msg.length) {
        int8 ch = msg[index];
        _ = ch;
        return print_at(msg, index + 1);
    }
    return 0;
};

int32 print = manual(string msg) -> print_at(msg, 0);

boot() -> print("hi");
